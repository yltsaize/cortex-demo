apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cortex-demo.fullname" . }}-test-connection"
  labels:
    {{- include "cortex-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cortex-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
