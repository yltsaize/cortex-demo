apiVersion: v1
kind: Service
metadata:
  name: {{ include "cortex-demo.fullname" . }}
  labels:
    {{- include "cortex-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cortex-demo.selectorLabels" . | nindent 4 }}
